language: c

matrix:
  include:
    - os: linux
      addons:
        apt:
          packages:
            - liblapack-dev
            - libblas-dev
            - libmetis-dev
            - gfortran
    - os: osx
      addons:
        homebrew:
          packages:
            - metis
    - os: windows

script:
  - ./build.sh
